apiVersion: v2
name: axonate
description: Self-hosted, OpenAI-compatible AI gateway — permanent control-plane services.
type: application
version: 0.1.0
appVersion: "1.0"
dependencies:
  - name: postgresql
    version: "15.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
