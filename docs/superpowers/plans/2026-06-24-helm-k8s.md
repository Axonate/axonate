# Helm Chart / Kubernetes Deploy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a prod-grade Helm chart at `deploy/helm/axonate/` that deploys Axonate's permanent services (litellm, router, cloudflared, optional in-cluster Postgres) to Kubernetes, preserving the OpenAI-compatible boundaries and the no-open-ports + Access-JWT security model.

**Architecture:** App chart with the Bitnami `postgresql` subchart as an optional dependency (default off → external managed PG). litellm config ships via ConfigMap; router config is baked into its image. One k8s Secret (or an `existingSecret`) feeds both services — a helper derives litellm's `DATABASE_URL` from the same Postgres parts the router consumes, so there is a single source of truth. cloudflared (Deployment, no Service) and Ingress (+cert-manager) are independent values toggles.

**Tech Stack:** Helm 3, Kubernetes 1.27+, Bitnami `postgresql` subchart, `ghcr.io/berriai/litellm` (digest-pinned), `cloudflare/cloudflared`, the project's own router image (built from `services/router/Dockerfile`, pushed to the operator's registry).

## Global Constraints

- Permanent services only. **Never** template the CLI adapter (POC-only, disposable, ToS single-user).
- Preserve "no open inbound ports": router/litellm Services are `ClusterIP`; default ingress path is the cloudflared tunnel. Ingress is opt-in.
- Reuse the digest pins already in `docker-compose.yml`: litellm `ghcr.io/berriai/litellm:main-stable@sha256:8f3517476b8293ccc1c3b0da7940d8dabbfe64b013454113f5a3cb68265c439b`; cloudflared tag `2025.6.1` (operator re-pins by digest at prod setup).
- Single source of truth for DB: Secret carries `postgres.{host,user,password,database}`; router gets the parts, litellm's `DATABASE_URL` is derived from them via a helper.
- Secrets are a plain k8s Secret from values **or** a referenced `existingSecret`. No ESO/sealed-secrets.
- Ports: router `4100` (health `/health`), litellm `4000` (health `/health/liveliness`).
- Every k8s object name prefixed via the chart fullname helper (keeps the `axonate-` convention).
- Do NOT build HPA/PDB/NetworkPolicy/ESO — document them as future in the chart README.

---

### Task 1: Chart scaffold — Chart.yaml, helpers, values, .helmignore

**Files:**
- Create: `deploy/helm/axonate/Chart.yaml`
- Create: `deploy/helm/axonate/.helmignore`
- Create: `deploy/helm/axonate/templates/_helpers.tpl`
- Create: `deploy/helm/axonate/values.yaml`

**Interfaces:**
- Produces: helpers `axonate.name`, `axonate.fullname`, `axonate.labels`, `axonate.selectorLabels`, `axonate.secretName` (returns `.Values.secret.existingSecret` if set, else `<fullname>`), `axonate.pgHost` (returns `<release>-postgresql` when `postgresql.enabled`, else `.Values.secret.postgres.host`), `axonate.databaseUrl` (builds `postgresql://user:pass@host:5432/db` from the resolved parts). All later tasks consume these.

- [ ] **Step 1: Write Chart.yaml**

```yaml
apiVersion: v2
name: axonate
description: Self-hosted, OpenAI-compatible AI gateway — permanent control-plane services.
type: application
version: 0.1.0
appVersion: "1.0"
dependencies:
  - name: postgresql
    version: "15.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
```

- [ ] **Step 2: Write .helmignore**

```
.DS_Store
*.md
ci/
.git/
.idea/
```

- [ ] **Step 3: Write templates/_helpers.tpl**

```yaml
{{- define "axonate.name" -}}
{{- default .Chart.Name .Values.nameOverride | trunc 63 | trimSuffix "-" -}}
{{- end -}}

{{- define "axonate.fullname" -}}
{{- if .Values.fullnameOverride -}}
{{- .Values.fullnameOverride | trunc 63 | trimSuffix "-" -}}
{{- else -}}
{{- printf "%s-%s" .Release.Name (include "axonate.name" .) | trunc 63 | trimSuffix "-" -}}
{{- end -}}
{{- end -}}

{{- define "axonate.labels" -}}
app.kubernetes.io/name: {{ include "axonate.name" . }}
app.kubernetes.io/instance: {{ .Release.Name }}
app.kubernetes.io/managed-by: {{ .Release.Service }}
helm.sh/chart: {{ printf "%s-%s" .Chart.Name .Chart.Version }}
{{- end -}}

{{- define "axonate.selectorLabels" -}}
app.kubernetes.io/name: {{ include "axonate.name" . }}
app.kubernetes.io/instance: {{ .Release.Name }}
{{- end -}}

{{- define "axonate.secretName" -}}
{{- if .Values.secret.existingSecret -}}
{{- .Values.secret.existingSecret -}}
{{- else -}}
{{- include "axonate.fullname" . -}}
{{- end -}}
{{- end -}}

{{- define "axonate.pgHost" -}}
{{- if .Values.postgresql.enabled -}}
{{- printf "%s-postgresql" .Release.Name -}}
{{- else -}}
{{- required "secret.postgres.host is required when postgresql.enabled=false" .Values.secret.postgres.host -}}
{{- end -}}
{{- end -}}

{{- define "axonate.databaseUrl" -}}
{{- $host := include "axonate.pgHost" . -}}
{{- printf "postgresql://%s:%s@%s:5432/%s" .Values.secret.postgres.user .Values.secret.postgres.password $host .Values.secret.postgres.database -}}
{{- end -}}
```

- [ ] **Step 4: Write values.yaml**

```yaml
nameOverride: ""
fullnameOverride: ""

image:
  litellm:
    repository: ghcr.io/berriai/litellm
    tag: main-stable
    digest: "sha256:8f3517476b8293ccc1c3b0da7940d8dabbfe64b013454113f5a3cb68265c439b"
    pullPolicy: IfNotPresent
  router:
    repository: ""            # REQUIRED: your registry, e.g. ghcr.io/you/axonate-router
    tag: v1.0
    digest: ""
    pullPolicy: IfNotPresent
  cloudflared:
    repository: cloudflare/cloudflared
    tag: "2025.6.1"
    digest: ""                # re-pin by digest at prod setup (GOLIVE §1)
    pullPolicy: IfNotPresent

litellm:
  configMode: prod            # poc|prod — selects which config the ConfigMap carries
  replicas: 1
  resources: {}

router:
  replicas: 2                 # stateless — safe to scale
  authMode: cloudflare        # dev|cloudflare
  devUser: dev@local
  logPrompts: false
  rateLimit: 60
  cfAccessTeamDomain: ""      # required when authMode=cloudflare
  cfAccessAud: ""             # required when authMode=cloudflare
  resources: {}

# Optional in-cluster Postgres (Bitnami subchart). Default OFF → external managed PG.
postgresql:
  enabled: false
  auth:
    username: axonate
    password: ""              # set when enabled; mirror into secret.postgres below
    database: axonate
  primary:
    persistence:
      enabled: true
      size: 8Gi

cloudflared:
  enabled: true               # default ingress path: tunnel, no open ports
  replicas: 1
  resources: {}

ingress:
  enabled: false              # opt-in standard ingress to the router Service
  className: nginx
  host: axonate.example.com
  annotations: {}
  tls:
    enabled: true
    clusterIssuer: letsencrypt-prod

backup:
  enabled: false              # only meaningful with in-cluster PG
  schedule: "0 3 * * *"
  image: postgres:16-alpine

serviceAccount:
  create: true
  name: ""

# Plain Secret created from these values UNLESS existingSecret is set.
secret:
  existingSecret: ""
  masterKey: ""
  saltKey: ""
  postgres:
    host: ""                  # external PG endpoint (ignored when postgresql.enabled=true)
    user: axonate
    password: ""
    database: axonate
  tunnelToken: ""             # cloudflared
  anthropicApiKey: ""
  openaiApiKey: ""
  deepseekApiKey: ""
  minimaxApiKey: ""
  minimaxBaseUrl: ""
```

- [ ] **Step 5: Add the Bitnami repo and build dependencies**

Run: `helm repo add bitnami https://charts.bitnami.com/bitnami && helm dependency build deploy/helm/axonate`
Expected: downloads `postgresql` into `deploy/helm/axonate/charts/`; `Chart.lock` created.

- [ ] **Step 6: Lint the scaffold**

Run: `helm lint deploy/helm/axonate`
Expected: `1 chart(s) linted, 0 chart(s) failed` (info-level messages about icon are fine).

- [ ] **Step 7: Commit**

```bash
git add deploy/helm/axonate/Chart.yaml deploy/helm/axonate/.helmignore deploy/helm/axonate/templates/_helpers.tpl deploy/helm/axonate/values.yaml deploy/helm/axonate/Chart.lock
echo "deploy/helm/axonate/charts/" >> .gitignore
git add .gitignore
git commit -m "Scaffold axonate Helm chart (Chart.yaml, helpers, values, PG dependency)"
```

---

### Task 2: Secret + ConfigMap templates

**Files:**
- Create: `deploy/helm/axonate/templates/secret.yaml`
- Create: `deploy/helm/axonate/templates/configmap.yaml`
- Create: `deploy/helm/axonate/templates/serviceaccount.yaml`

**Interfaces:**
- Consumes: helpers from Task 1; the litellm config files at `services/litellm/litellm_config.yaml` and `services/litellm/litellm_config.prod.yaml`.
- Produces: a Secret named by `axonate.secretName` with keys `LITELLM_MASTER_KEY`, `LITELLM_SALT_KEY`, `DATABASE_URL`, `POSTGRES_HOST/USER/PASSWORD/DB`, `CLOUDFLARE_TUNNEL_TOKEN`, provider keys; a ConfigMap `<fullname>-litellm-config` with key `config.yaml`; a ServiceAccount.

- [ ] **Step 1: Write serviceaccount.yaml**

```yaml
{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "axonate.fullname" .) }}
  labels:
    {{- include "axonate.labels" . | nindent 4 }}
{{- end }}
```

- [ ] **Step 2: Write secret.yaml**

```yaml
{{- if not .Values.secret.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "axonate.fullname" . }}
  labels:
    {{- include "axonate.labels" . | nindent 4 }}
type: Opaque
stringData:
  LITELLM_MASTER_KEY: {{ required "secret.masterKey is required" .Values.secret.masterKey | quote }}
  LITELLM_SALT_KEY: {{ required "secret.saltKey is required" .Values.secret.saltKey | quote }}
  DATABASE_URL: {{ include "axonate.databaseUrl" . | quote }}
  POSTGRES_HOST: {{ include "axonate.pgHost" . | quote }}
  POSTGRES_USER: {{ .Values.secret.postgres.user | quote }}
  POSTGRES_PASSWORD: {{ .Values.secret.postgres.password | quote }}
  POSTGRES_DB: {{ .Values.secret.postgres.database | quote }}
  CLOUDFLARE_TUNNEL_TOKEN: {{ .Values.secret.tunnelToken | quote }}
  ANTHROPIC_API_KEY: {{ .Values.secret.anthropicApiKey | quote }}
  OPENAI_API_KEY: {{ .Values.secret.openaiApiKey | quote }}
  DEEPSEEK_API_KEY: {{ .Values.secret.deepseekApiKey | quote }}
  MINIMAX_API_KEY: {{ .Values.secret.minimaxApiKey | quote }}
  MINIMAX_BASE_URL: {{ .Values.secret.minimaxBaseUrl | quote }}
{{- end }}
```

- [ ] **Step 3: Write configmap.yaml (loads the matching litellm config file at template time)**

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "axonate.fullname" . }}-litellm-config
  labels:
    {{- include "axonate.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ if eq .Values.litellm.configMode "poc" }}{{ .Files.Get "files/litellm_config.yaml" | indent 4 }}{{ else }}{{ .Files.Get "files/litellm_config.prod.yaml" | indent 4 }}{{ end }}
```

- [ ] **Step 4: Make the litellm config files available to the chart**

Helm `.Files.Get` can only read files inside the chart dir. Copy them in:

Run: `mkdir -p deploy/helm/axonate/files && cp services/litellm/litellm_config.yaml services/litellm/litellm_config.prod.yaml deploy/helm/axonate/files/`
Expected: both files now under `deploy/helm/axonate/files/`.

- [ ] **Step 5: Render to verify the Secret + ConfigMap produce valid YAML**

Run:
```bash
helm template t deploy/helm/axonate \
  --set image.router.repository=example/router \
  --set secret.masterKey=sk-test --set secret.saltKey=salt-test \
  --set secret.postgres.host=db.example.com --set secret.postgres.password=pw \
  --show-only templates/secret.yaml --show-only templates/configmap.yaml
```
Expected: a `kind: Secret` with `DATABASE_URL: "postgresql://axonate:pw@db.example.com:5432/axonate"` and a `kind: ConfigMap` whose `config.yaml` contains the litellm model list. No template errors.

- [ ] **Step 6: Commit**

```bash
git add deploy/helm/axonate/templates/secret.yaml deploy/helm/axonate/templates/configmap.yaml deploy/helm/axonate/templates/serviceaccount.yaml deploy/helm/axonate/files/
git commit -m "Add Helm Secret, litellm ConfigMap, and ServiceAccount"
```

---

### Task 3: litellm Deployment + Service

**Files:**
- Create: `deploy/helm/axonate/templates/litellm-deployment.yaml`
- Create: `deploy/helm/axonate/templates/litellm-service.yaml`

**Interfaces:**
- Consumes: Secret (`axonate.secretName`), ConfigMap `<fullname>-litellm-config`, helpers.
- Produces: Service `<fullname>-litellm` (ClusterIP, port 4000) that the router and ingress target.

- [ ] **Step 1: Write litellm-deployment.yaml**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "axonate.fullname" . }}-litellm
  labels:
    {{- include "axonate.labels" . | nindent 4 }}
    app.kubernetes.io/component: litellm
spec:
  replicas: {{ .Values.litellm.replicas }}
  selector:
    matchLabels:
      {{- include "axonate.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: litellm
  template:
    metadata:
      labels:
        {{- include "axonate.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: litellm
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name | default (include "axonate.fullname" .) }}
      containers:
        - name: litellm
          image: "{{ .Values.image.litellm.repository }}{{ if .Values.image.litellm.digest }}@{{ .Values.image.litellm.digest }}{{ else }}:{{ .Values.image.litellm.tag }}{{ end }}"
          imagePullPolicy: {{ .Values.image.litellm.pullPolicy }}
          args: ["--config", "/app/config.yaml", "--port", "4000"]
          envFrom:
            - secretRef:
                name: {{ include "axonate.secretName" . }}
          ports:
            - name: http
              containerPort: 4000
          volumeMounts:
            - name: config
              mountPath: /app/config.yaml
              subPath: config.yaml
              readOnly: true
          readinessProbe:
            httpGet: { path: /health/liveliness, port: 4000 }
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 60      # first boot runs ~128 DB migrations (minutes)
          livenessProbe:
            httpGet: { path: /health/liveliness, port: 4000 }
            initialDelaySeconds: 120
            periodSeconds: 15
          resources: {{- toYaml .Values.litellm.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "axonate.fullname" . }}-litellm-config
```

- [ ] **Step 2: Write litellm-service.yaml**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ include "axonate.fullname" . }}-litellm
  labels:
    {{- include "axonate.labels" . | nindent 4 }}
    app.kubernetes.io/component: litellm
spec:
  type: ClusterIP
  selector:
    {{- include "axonate.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: litellm
  ports:
    - name: http
      port: 4000
      targetPort: 4000
```

- [ ] **Step 3: Render and grep for the derived image + probe**

Run:
```bash
helm template t deploy/helm/axonate \
  --set image.router.repository=example/router \
  --set secret.masterKey=k --set secret.saltKey=s \
  --set secret.postgres.host=h --set secret.postgres.password=p \
  --show-only templates/litellm-deployment.yaml
```
Expected: image string ends with `@sha256:8f35...` (digest wins over tag); `path: /health/liveliness`; `envFrom` references the secret.

- [ ] **Step 4: Commit**

```bash
git add deploy/helm/axonate/templates/litellm-deployment.yaml deploy/helm/axonate/templates/litellm-service.yaml
git commit -m "Add litellm Deployment + ClusterIP Service"
```

---

### Task 4: router Deployment + Service

**Files:**
- Create: `deploy/helm/axonate/templates/router-deployment.yaml`
- Create: `deploy/helm/axonate/templates/router-service.yaml`

**Interfaces:**
- Consumes: Secret, litellm Service DNS `<fullname>-litellm:4000`, helpers.
- Produces: Service `<fullname>-router` (ClusterIP, port 4100) that cloudflared and ingress target.

- [ ] **Step 1: Write router-deployment.yaml**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "axonate.fullname" . }}-router
  labels:
    {{- include "axonate.labels" . | nindent 4 }}
    app.kubernetes.io/component: router
spec:
  replicas: {{ .Values.router.replicas }}
  selector:
    matchLabels:
      {{- include "axonate.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: router
  template:
    metadata:
      labels:
        {{- include "axonate.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: router
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name | default (include "axonate.fullname" .) }}
      containers:
        - name: router
          image: "{{ required "image.router.repository is required" .Values.image.router.repository }}{{ if .Values.image.router.digest }}@{{ .Values.image.router.digest }}{{ else }}:{{ .Values.image.router.tag }}{{ end }}"
          imagePullPolicy: {{ .Values.image.router.pullPolicy }}
          env:
            - name: LITELLM_URL
              value: "http://{{ include "axonate.fullname" . }}-litellm:4000"
            - name: AUTH_MODE
              value: {{ .Values.router.authMode | quote }}
            - name: ROUTER_DEV_USER
              value: {{ .Values.router.devUser | quote }}
            - name: LOG_PROMPTS
              value: {{ .Values.router.logPrompts | quote }}
            - name: ROUTER_RATE_LIMIT
              value: {{ .Values.router.rateLimit | quote }}
            - name: CF_ACCESS_TEAM_DOMAIN
              value: {{ .Values.router.cfAccessTeamDomain | quote }}
            - name: CF_ACCESS_AUD
              value: {{ .Values.router.cfAccessAud | quote }}
          envFrom:
            - secretRef:
                name: {{ include "axonate.secretName" . }}
          ports:
            - name: http
              containerPort: 4100
          readinessProbe:
            httpGet: { path: /health, port: 4100 }
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 4100 }
            initialDelaySeconds: 20
            periodSeconds: 15
          resources: {{- toYaml .Values.router.resources | nindent 12 }}
```

(The Secret supplies `LITELLM_MASTER_KEY`, `POSTGRES_HOST/USER/PASSWORD/DB` via `envFrom`; the explicit `env` block adds the router-only non-secret config.)

- [ ] **Step 2: Write router-service.yaml**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ include "axonate.fullname" . }}-router
  labels:
    {{- include "axonate.labels" . | nindent 4 }}
    app.kubernetes.io/component: router
spec:
  type: ClusterIP
  selector:
    {{- include "axonate.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: router
  ports:
    - name: http
      port: 4100
      targetPort: 4100
```

- [ ] **Step 3: Render and verify wiring + required-image guard**

Run (missing router image → should fail):
```bash
helm template t deploy/helm/axonate --set secret.masterKey=k --set secret.saltKey=s --set secret.postgres.host=h --set secret.postgres.password=p --show-only templates/router-deployment.yaml
```
Expected: FAIL with `image.router.repository is required`.

Then run with it set:
```bash
helm template t deploy/helm/axonate --set image.router.repository=example/router --set secret.masterKey=k --set secret.saltKey=s --set secret.postgres.host=h --set secret.postgres.password=p --show-only templates/router-deployment.yaml
```
Expected: `LITELLM_URL` = `http://t-axonate-litellm:4000`; `envFrom` secretRef present; `path: /health`.

- [ ] **Step 4: Commit**

```bash
git add deploy/helm/axonate/templates/router-deployment.yaml deploy/helm/axonate/templates/router-service.yaml
git commit -m "Add router Deployment + ClusterIP Service"
```

---

### Task 5: cloudflared Deployment + Ingress (both toggled) + no-ingress guard

**Files:**
- Create: `deploy/helm/axonate/templates/cloudflared-deployment.yaml`
- Create: `deploy/helm/axonate/templates/ingress.yaml`

**Interfaces:**
- Consumes: Secret (`CLOUDFLARE_TUNNEL_TOKEN`), router Service `<fullname>-router:4100`, helpers.
- Produces: tunnel Deployment (no Service); optional Ingress to the router Service.

- [ ] **Step 1: Write cloudflared-deployment.yaml**

```yaml
{{- if .Values.cloudflared.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "axonate.fullname" . }}-cloudflared
  labels:
    {{- include "axonate.labels" . | nindent 4 }}
    app.kubernetes.io/component: cloudflared
spec:
  replicas: {{ .Values.cloudflared.replicas }}
  selector:
    matchLabels:
      {{- include "axonate.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: cloudflared
  template:
    metadata:
      labels:
        {{- include "axonate.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: cloudflared
    spec:
      containers:
        - name: cloudflared
          image: "{{ .Values.image.cloudflared.repository }}{{ if .Values.image.cloudflared.digest }}@{{ .Values.image.cloudflared.digest }}{{ else }}:{{ .Values.image.cloudflared.tag }}{{ end }}"
          imagePullPolicy: {{ .Values.image.cloudflared.pullPolicy }}
          args: ["tunnel", "--no-autoupdate", "run"]
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "axonate.secretName" . }}
                  key: CLOUDFLARE_TUNNEL_TOKEN
          resources: {{- toYaml .Values.cloudflared.resources | nindent 12 }}
{{- end }}
```

(The Cloudflare tunnel config routes the public hostname to the in-cluster router Service `http://<fullname>-router:4100` — configured on the Cloudflare dashboard side, same as the compose model.)

- [ ] **Step 2: Write ingress.yaml**

```yaml
{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "axonate.fullname" . }}
  labels:
    {{- include "axonate.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.ingress.annotations }}{{- toYaml . | nindent 4 }}{{- end }}
    {{- if .Values.ingress.tls.enabled }}
    cert-manager.io/cluster-issuer: {{ .Values.ingress.tls.clusterIssuer | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts: [{{ .Values.ingress.host | quote }}]
      secretName: {{ include "axonate.fullname" . }}-tls
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "axonate.fullname" . }}-router
                port:
                  number: 4100
{{- end }}
```

- [ ] **Step 3: Render the default (cloudflared on, ingress off)**

Run:
```bash
helm template t deploy/helm/axonate --set image.router.repository=example/router --set secret.masterKey=k --set secret.saltKey=s --set secret.postgres.host=h --set secret.postgres.password=p --set secret.tunnelToken=tok | grep -E "kind: (Deployment|Ingress)"
```
Expected: three Deployments (litellm, router, cloudflared); **no** Ingress.

- [ ] **Step 4: Render the ingress path**

Run: add `--set ingress.enabled=true --set cloudflared.enabled=false` to the previous command.
Expected: an `Ingress` to service `t-axonate-router` port `4100`; no cloudflared Deployment.

- [ ] **Step 5: Commit**

```bash
git add deploy/helm/axonate/templates/cloudflared-deployment.yaml deploy/helm/axonate/templates/ingress.yaml
git commit -m "Add cloudflared Deployment and optional Ingress (values-toggled)"
```

---

### Task 6: backup CronJob, NOTES.txt, chart README

**Files:**
- Create: `deploy/helm/axonate/templates/backup-cronjob.yaml`
- Create: `deploy/helm/axonate/templates/NOTES.txt`
- Create: `deploy/helm/axonate/README.md`

**Interfaces:**
- Consumes: Secret (`POSTGRES_*`), helpers.
- Produces: a CronJob rendered only when `postgresql.enabled && backup.enabled`; install notes; values reference doc.

- [ ] **Step 1: Write backup-cronjob.yaml**

```yaml
{{- if and .Values.postgresql.enabled .Values.backup.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "axonate.fullname" . }}-backup
  labels:
    {{- include "axonate.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.backup.schedule | quote }}
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: pg-dump
              image: {{ .Values.backup.image | quote }}
              command: ["/bin/sh", "-c"]
              args:
                - 'pg_dump "$DATABASE_URL" | gzip > /backups/axonate-$(date +%F).sql.gz'
              envFrom:
                - secretRef:
                    name: {{ include "axonate.secretName" . }}
              volumeMounts:
                - name: backups
                  mountPath: /backups
          volumes:
            - name: backups
              emptyDir: {}     # replace with a PVC for durable storage
{{- end }}
```

(Mirrors `scripts/backup.sh`. External managed PG uses the cloud provider's backups, so this only renders for in-cluster PG. The `emptyDir` is a placeholder — the README documents swapping in a PVC.)

- [ ] **Step 2: Write NOTES.txt**

```
Axonate {{ .Chart.AppVersion }} deployed as release "{{ .Release.Name }}".

Services (ClusterIP, no open ports):
  router : {{ include "axonate.fullname" . }}-router:4100   (health /health)
  litellm: {{ include "axonate.fullname" . }}-litellm:4000  (health /health/liveliness)

{{- if .Values.cloudflared.enabled }}
Ingress path: Cloudflare tunnel (cloudflared Deployment). Point the tunnel's public
hostname at http://{{ include "axonate.fullname" . }}-router:4100 in the Cloudflare dashboard.
{{- else if .Values.ingress.enabled }}
Ingress path: https://{{ .Values.ingress.host }}  ->  router Service.
{{- else }}
WARNING: neither cloudflared nor ingress is enabled — the gateway has NO external path.
{{- end }}

First boot: litellm runs ~128 DB migrations (several minutes). Watch:
  kubectl rollout status deploy/{{ include "axonate.fullname" . }}-litellm

Smoke test via port-forward:
  kubectl port-forward svc/{{ include "axonate.fullname" . }}-router 4100:4100
  curl -s localhost:4100/health
```

- [ ] **Step 3: Write README.md**

````markdown
# Axonate Helm chart

Deploys Axonate's **permanent** control-plane services — litellm, router, cloudflared,
and (optionally) in-cluster Postgres. The disposable POC CLI adapter is intentionally
**not** part of this chart.

## Prerequisites

- Build and push the router image from the repo root:
  `docker build -f services/router/Dockerfile -t <registry>/axonate-router:v1.0 .` then push.
- A Postgres database: either external managed (default) or in-cluster (`postgresql.enabled=true`).

## Install

```bash
helm repo add bitnami https://charts.bitnami.com/bitnami
helm dependency build deploy/helm/axonate

helm install axonate deploy/helm/axonate \
  --set image.router.repository=<registry>/axonate-router \
  --set secret.masterKey=sk-... --set secret.saltKey=... \
  --set secret.postgres.host=<rds-endpoint> \
  --set secret.postgres.user=axonate --set secret.postgres.password=... \
  --set secret.postgres.database=axonate \
  --set secret.tunnelToken=<cloudflare-tunnel-token> \
  --set router.cfAccessTeamDomain=<team>.cloudflareaccess.com \
  --set router.cfAccessAud=<aud>
```

## Key values

| Value | Default | Purpose |
|---|---|---|
| `image.router.repository` | "" (required) | Your pushed router image |
| `litellm.configMode` | `prod` | `poc` or `prod` litellm config (from `files/`) |
| `postgresql.enabled` | `false` | In-cluster Bitnami PG vs external managed |
| `cloudflared.enabled` | `true` | Tunnel ingress (no open ports) |
| `ingress.enabled` | `false` | Standard Ingress + cert-manager to the router |
| `secret.existingSecret` | "" | Reference a Secret you created out-of-band |
| `backup.enabled` | `false` | pg_dump CronJob (in-cluster PG only) |

## Secrets

By default the chart creates one k8s Secret from `secret.*` values. For production,
prefer creating the Secret out-of-band (CI / a secret manager) and setting
`secret.existingSecret=<name>` with the same keys
(`LITELLM_MASTER_KEY`, `LITELLM_SALT_KEY`, `DATABASE_URL`, `POSTGRES_*`,
`CLOUDFLARE_TUNNEL_TOKEN`, provider keys).

## External vs in-cluster Postgres

- **External (default):** set `secret.postgres.{host,user,password,database}`; the chart
  builds litellm's `DATABASE_URL` and the router's `POSTGRES_*` from those.
- **In-cluster:** `--set postgresql.enabled=true` plus matching `postgresql.auth.*` and
  `secret.postgres.*`. The Bitnami subchart provisions PG with a PVC. Enable `backup.enabled`
  for the pg_dump CronJob (swap the `emptyDir` for a PVC for durable backups).

## Backup CronJob

Only rendered with in-cluster PG. Mirrors `scripts/backup.sh`. The default `emptyDir`
volume is ephemeral — mount a PersistentVolumeClaim for real backups.

## Not built yet (future)

HorizontalPodAutoscaler, PodDisruptionBudget, NetworkPolicy, External Secrets Operator /
sealed-secrets, and multi-environment overlays are intentionally out of scope for this
chart version. Secrets are plain k8s Secrets (or `existingSecret`) until then.
````

- [ ] **Step 4: Full-chart lint + render across all paths**

Run:
```bash
helm lint deploy/helm/axonate
# default (external PG, cloudflared)
helm template t deploy/helm/axonate --set image.router.repository=ex/r --set secret.masterKey=k --set secret.saltKey=s --set secret.postgres.host=h --set secret.postgres.password=p --set secret.tunnelToken=tok > /tmp/axonate-default.yaml
# in-cluster PG + backup + ingress
helm template t deploy/helm/axonate --set image.router.repository=ex/r --set secret.masterKey=k --set secret.saltKey=s --set secret.postgres.password=p --set postgresql.enabled=true --set postgresql.auth.password=p --set backup.enabled=true --set ingress.enabled=true > /tmp/axonate-incluster.yaml
echo "rendered OK"
```
Expected: lint passes; both renders succeed; `/tmp/axonate-incluster.yaml` contains a `CronJob`, an `Ingress`, and Bitnami `postgresql` objects; `/tmp/axonate-default.yaml` contains none of those but does contain the cloudflared Deployment.

- [ ] **Step 5: (optional, if a cluster is available) install against kind/minikube**

Run:
```bash
helm install axonate deploy/helm/axonate --set image.router.repository=ex/r --set secret.masterKey=k --set secret.saltKey=s --set secret.postgres.password=p --set postgresql.enabled=true --set postgresql.auth.password=p --set cloudflared.enabled=false
kubectl rollout status deploy/axonate-axonate-litellm --timeout=600s
```
Expected: PG comes up, litellm migrates and becomes ready, router becomes ready. (Skip if no cluster; the template renders are the gating check.)

- [ ] **Step 6: Commit**

```bash
git add deploy/helm/axonate/templates/backup-cronjob.yaml deploy/helm/axonate/templates/NOTES.txt deploy/helm/axonate/README.md
git commit -m "Add backup CronJob, NOTES, and chart README"
```

---

## Self-Review

- **Spec coverage:** permanent-services-only/no-adapter → Global Constraints + every template; external-default/in-cluster-optional PG → Task 1 helpers + Task 6 README + `postgresql.enabled`; cloudflared default + ingress toggle (both values-toggled) → Task 5; plain Secret or `existingSecret` → Task 2 + helper `axonate.secretName`; ConfigMap for litellm config (poc|prod) → Task 2; digest pins reused → values.yaml + Task 3 render check; readiness for slow first-boot migrations → Task 3 `failureThreshold: 60`; backup CronJob only with in-cluster PG → Task 6 guard; non-goals (HPA/PDB/NetworkPolicy/ESO) documented not built → Task 6 README. Acceptance checks 1–6 from the spec map to the lint/template/install steps in Tasks 1, 3, 4, 5, 6.
- **Refinement vs spec:** the spec listed `externalDatabase.databaseUrl`; the plan instead derives `DATABASE_URL` from `secret.postgres.*` via the `axonate.databaseUrl` helper so the router's `POSTGRES_*` and litellm's `DATABASE_URL` share one source of truth. Functionally equivalent, less drift-prone.
- **Placeholder scan:** none — every template and doc is complete; the `emptyDir` in the backup CronJob is an explicit, documented default, not a placeholder.
- **Type/name consistency:** Service names `<fullname>-litellm` / `<fullname>-router` are referenced identically in the router env (`LITELLM_URL`), ingress backend, NOTES, and the cloudflared note. `axonate.secretName` / `axonate.pgHost` / `axonate.databaseUrl` helper names match every call site. Secret keys (`LITELLM_MASTER_KEY`, `POSTGRES_*`, `DATABASE_URL`, `CLOUDFLARE_TUNNEL_TOKEN`) are consistent between `secret.yaml`, the deployments' `envFrom`, cloudflared's `secretKeyRef`, and the README.
